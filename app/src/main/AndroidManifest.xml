<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.mad.footballtest">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        android:name=".App"
        >
        <activity android:name=".mvp.screens.leagueslist.LeaguesListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".mvp.screens.commandplayers.CommandPlayersActivity"/>
        <activity android:name=".mvp.screens.commandprofile.CommandProfileActivity"/>
        <activity android:name=".mvp.screens.leagueprofile.LeagueProfileActivity"/>
        <activity android:name=".mvp.screens.leaguetable.LeagueTableActivity"/>
    </application>

</manifest>